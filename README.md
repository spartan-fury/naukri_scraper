Firstly, run save_naukri_cookies.py and login using your own credentials i.e. email id. This allows to allow future logins while using undected chromedriver.

Then, run naukri_scraper.py to scrape the jobs.
